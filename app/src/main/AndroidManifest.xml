<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tute4"
        tools:targetApi="31">


            <activity android:name=".LogoActivity"

                android:exported="true">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />
                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
            </activity>
            <activity android:name=".SignupActivity" />
            <activity android:name=".SigninActivity" />
            <activity android:name=".HomepageActivity" />
            <activity android:name=".ProfileActivity" />
            <activity android:name=".SettingActivity" />
            <activity android:name=".NotificationActivity" />
            <activity android:name=".Payment1Activity" />
            <activity android:name=".ForgetPasswordActivity" />
            <activity android:name=".Onboard1Activity" />
            <activity android:name=".Onboard3Activity" />
            <activity android:name=".Page1Activity" />
            <activity android:name=".Page2Activity" />
            <activity android:name=".Page3Activity" />
            <activity android:name=".Payment2Activity" />
            <activity android:name=".Payment3Activity" />
            <activity android:name=".WelcomeActivity" />
        </application>



    </manifest>